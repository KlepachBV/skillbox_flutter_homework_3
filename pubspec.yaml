name: skillbox_flutter_homework_3
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.15.0-116.0.dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^0.23.0+1

  carousel_slider:
    git:
      url: https://github.com/serenader2014/flutter_carousel_slider
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  import_sorter: ^4.6.0



flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
    - assets/fonts/

